import { StudySessionLog } from "../types";

/**
 * Returns clean initial study sessions (empty by default; no mock sessions)
 */
export const getInitialStudySessions = (): StudySessionLog[] => {
  return [];
};

